{% extends '_layout.njk' %}

{% block title %}{{ page.title }} | {{ config.title }}{% endblock %}

{% block content %}

  <div class="post-content">
    <h1>{{ page.title }}</h1>
    {{ page.content }}
  </div>

{% endblock %}
