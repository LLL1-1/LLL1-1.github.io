# 本项目专用 npm 配置。
#
# 为什么需要：本机访问 registry.npmjs.org 被重置（Connection was reset），
# 而 package-lock.json 里 248 个依赖的 resolved 地址全部指向 registry.npmjs.org，
# 所以 `npm install` 会失败。
#
# 使用国内镜像可正常安装。注意：镜像的包内容与官方一致，但如你所在网络能直连
# 官方源，或公司要求走内网源，请删掉本文件或改成你的源。
registry=https://registry.npmmirror.com
